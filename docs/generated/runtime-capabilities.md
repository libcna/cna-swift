# Runtime capabilities

<!-- Generated by tools/runtime_capabilities/render.py from
     docs/runtime-capabilities.json. Do not edit by hand. -->

Qualified boundary: Swift 6.0.3, `x86_64-pc-linux-gnu`,
Linux x86-64, canonical CNA C ABI 0.21.0
(admitted window: major 0, minor >= 21),
HEADLESS renderer, SDL3 audio.

CNA source revision `0a6158e4ff764907065cd7259e3d29e331a52088`, library SHA-256
`c32bfbd307d695664f906ccf2834ec3f9ebc240fa388d544ac21ee3ebaeb731b`.

| Operation | Status | Evidence |
|---|---|---|
| XNA namespace projection | VERIFIED_MANAGED | Swift Symbol Graph emits the strict Microsoft.Xna.Framework path; marker symbols are measured exclusions. |
| Pure math/value foundation | VERIFIED_MANAGED | MathHelper, Point, Rectangle, GameTime, the binary32/geometry closure, Color, and both packed-vector protocols are strict-complete managed Swift. |
| DisplayOrientation | VERIFIED_MANAGED | Exact root-framework Int32 OptionSet with Default=0, LandscapeLeft=1, LandscapeRight=2, and Portrait=4; no runtime orientation claim. |
| BufferUsage | VERIFIED_MANAGED | Exact Graphics Int32 OptionSet with None=0 and WriteOnly=1; no buffer or GPU resource capability is claimed. |
| DepthFormat | VERIFIED_MANAGED | Exact Graphics non-flags Int32 enum with None=0, Depth16=1, Depth24=2, and Depth24Stencil8=3; no depth buffer, stencil, render-target, GPU-format, or native capability is claimed. |
| FillMode | VERIFIED_MANAGED | Exact Graphics non-flags Int32 enum with Solid=0 and WireFrame=1; no RasterizerState, polygon mode, or wireframe rendering capability is claimed. |
| SurfaceFormat | VERIFIED_MANAGED | Exact Graphics non-flags Int32 enum with all twenty pinned literals; no texture, render-target, display, GPU-format, DXT, HDR, or native capability is claimed. |
| RenderTargetUsage managed enum contract | VERIFIED_MANAGED | Exact Graphics non-flags Int32 enum with DiscardContents=0, PreserveContents=1, and PlatformContents=2; no RenderTarget2D, RenderTargetCube, PresentationParameters, GraphicsDevice render-target operation, content discard/preserve/platform-defined semantics, or native capability is claimed. |
| GraphicsProfile managed enum contract | VERIFIED_MANAGED | Exact Graphics non-flags Int32 enum with Reach=0 and HiDef=1; no profile selection, GraphicsAdapter, GraphicsDeviceInformation, capability negotiation, or native capability is claimed. |
| Presentation and device-status managed enum contracts | VERIFIED_MANAGED | Exact Graphics enums PresentInterval (Default=0..Immediate=3), GraphicsDeviceStatus (Normal=0, Lost=1, NotReset=2) and PrimitiveType (TriangleList=0..LineStrip=3), plus the ClearOptions Int32 OptionSet (Target=1, DepthBuffer=2, Stencil=4); no present interval, vsync, swap chain, device-lost handling, device reset, Clear option path, DrawPrimitives, or native capability is claimed. |
| Render-state managed enum contracts | VERIFIED_MANAGED | Exact Graphics enums Blend, BlendFunction, CompareFunction, StencilOperation, CullMode, TextureFilter and TextureAddressMode, plus the ColorWriteChannels and SetDataOptions Int32 OptionSets; no BlendState, DepthStencilState, RasterizerState, SamplerState, blending, depth/stencil testing, culling, filtering, buffer update, or native capability is claimed. |
| Vertex declaration managed contracts | VERIFIED_MANAGED | Exact Graphics enums VertexElementFormat, VertexElementUsage and IndexElementSize, plus the VertexElement value struct with verbatim unvalidated construction, four-field equality, SmartGetHashCode word XOR and the exact reference ToString; no VertexDeclaration, IVertexType, VertexBuffer, IndexBuffer, vertex submission, or native vertex-format capability is claimed. |
| CubeMapFace managed enum contract | VERIFIED_MANAGED | Exact Graphics non-flags Int32 enum with PositiveX=0 through NegativeZ=5; no TextureCube, RenderTargetBinding, cube render target, cube sampling, or native face-mapping capability is claimed. |
| Effect parameter and effect interface managed contracts | VERIFIED_MANAGED | Exact Graphics enums EffectParameterClass and EffectParameterType, plus the IEffectFog and IEffectMatrices protocols with their exact abstract read/write property sets and no conformer; no Effect, EffectParameter, EffectAnnotation, BasicEffect, shader compilation, fog, applied matrix, or native capability is claimed. |
| Audio managed enum contracts | VERIFIED_MANAGED | Exact Audio non-flags Int32 enums SoundState (Playing=0, Paused=1, Stopped=2) and AudioChannels (Mono=1, Stereo=2, no zero literal). AudioListener and AudioEmitter exist as pure managed 3D data that reproduce the pinned XACT handedness flip and hold no engine handle; no SoundEffect, SoundEffectInstance, Cue, playback, mixing, XACT interop or audio device capability is claimed, and the audio backend remains NULL. |
| CLR property accessor projection | VERIFIED_MANAGED | One accessor-level rule for every CLR property: the getter keeps Swift property or subscript syntax and gains `get throws` exactly when the XNA getter is fallible, and a setter Swift cannot express becomes Set<PropertyName> carrying the setter's own throws. Fallibility is derived from the CIL of the seven registered assemblies and pinned as tools/api_compat/reference/xna40-accessor-fallibility.json -- 114 fallible getters and 113 fallible setters of 840 properties -- and hash-checked by the verifier. This is a language mapping only: it claims no runtime, device or native behaviour, and the projected writer is the CLR setter accessor, not an added XNA member. |
| AudioEmitter managed 3D data | VERIFIED_MANAGED | The pinned XACT_EMITTER_DATA storage, the FlipHandedness involution and its negative-zero default Position.Z and Velocity.Z, and the exact ordered bge.un DopplerScale boundary in which NaN and -0.0 are accepted while every ordered negative throws. No XACT engine, voice, listener pairing or audio device is involved; Cue.Apply3D is not implemented, so nothing consumes an emitter and constructing one claims no audio capability. |
| DisplayMode managed descriptor contract | VERIFIED_MANAGED | Exact Graphics class with six pinned identities, no public constructor, internal-only construction, verbatim Width/Height/Format storage, guarded binary32 AspectRatio, unmodified Windows TitleSafeArea rectangle, and exact ToString; no monitor enumeration, display-mode discovery, resolution switching, GraphicsAdapter, GraphicsDevice.DisplayMode, fullscreen mode management, or native display capability is claimed. |
| Game lifecycle | VERIFIED_NATIVE | Initialize/LoadContent/BeginRun/Update/Draw/EndRun/UnloadContent/Exiting execute through CNA callbacks; 60/600 runs pass. |
| Game callback error containment | VERIFIED_NATIVE | Initialize, LoadContent, Update, Draw and UnloadContent errors are caught in C trampolines and rethrown at controlled Swift boundaries. |
| Game recreation | VERIFIED_NATIVE | 20 create/run/exit/destroy cycles pass; stale generations reject use. |
| GraphicsDevice | VERIFIED_NATIVE | Callback-scoped borrowed device returned by cna_game_get_graphics_device. |
| Clear | VERIFIED_NATIVE | All three XNA overloads execute through cna_graphics_device_clear_options; the manager-created device reports DepthFormat.Depth24, so Clear(Color) forwards Target\|DepthBuffer. |
| Texture2D stream load | VERIFIED_NATIVE | Foundation.InputStream bytes decode through CNA; logo reports native 128x128 and stress fixture reports 1x1. |
| Texture2D PNG/JPEG encoding | VERIFIED_NATIVE | SaveAsPng and SaveAsJpeg execute through cna_texture2d_create_cpu_only_rgba8, _get_encoded_byte_count and _copy_encoded, and the emitted bytes are read back by a PNG decoder written from the format specification in the test target, which knows nothing about CNA: the authored texels come out texel for texel, a non-square target size appears in the IHDR, and the JPEG's own frame header carries the requested dimensions. XNA's rewrite of every alpha-zero texel to Color.Transparent is performed by this projection because CNA's encoder does not: build-probe/f59_encode.c authored (200,100,50,0) and an independent decode read (200,100,50,0) back. cna_texture2d_save_file exists and is deliberately unbound; no XNA member takes a path. |
| Texture2D decode resize and zoom | MEASURED_NATIVE_DIVERGENCE | FromStream(device, stream, width, height, zoom) passes CNA_Texture2DDecodeInfo, whose zoom flag is documented as cover-and-crop against fit -- the two operations XNA selects with XnaImageOperation.Scale\|Crop and Scale. XNA's own resize is inside UnsafeNativeMethods.DecodeStreamToTexture, unmanaged code absent from the registered assembly, so there is NO pinned authority for the arithmetic and none is claimed. Measured in build-probe/f59_decode.c over sixteen targets from a 4x2 source: zoom=false ignores the requested HEIGHT entirely -- it grants the requested width with the height derived from the source aspect, so 8x2 grants 8x4 -- where XNA's Scale fits inside both; and zoom=true refuses 8x2 and 2x8 with CNA_RESULT_INVALID_ARGUMENT while granting 4x4, 5x5, 3x6, 4x8 and 16x8 exactly. Both are reported, not corrected: correcting them would replace a measured native operation with an invented managed resampler. |
| Dispose(Boolean) on the finalizer path | VERIFIED_MANAGED | GraphicsResource.Dispose(false) disposes and raises nothing, which is what the IL does: the disposing branch calls ~GraphicsResource() -- flag then Disposing -- and the finalizer branch calls !GraphicsResource() and Object.Finalize(), neither of which touches the delegate. Until Foundation 59 the projection raised the event on both paths, and Swift's lack of protected puts Dispose(false) within any consumer's reach. Texture2D and SpriteBatch now declare the override XNA declares, and RenderTarget2D's three-deep chain still releases its native content-lost subscription before the base disposes. |
| Vertex and index buffer contents | VERIFIED_NATIVE | VertexBuffer and IndexBuffer create through cna_vertex_buffer_create and cna_index_buffer_create and round-trip their contents: authored vertices come back vertex for vertex through cna_vertex_buffer_get_data_raw, indices at both widths through cna_index_buffer_get_data, and a windowed write at a byte offset leaves the rest of the buffer alone. A WriteOnly buffer refuses GetData with XNA's own NotSupportedException before the native call, which CNA also refuses natively. Nothing DRAWN with these buffers is claimed. |
| Vertex transfer stride | MEASURED_NATIVE_DIVERGENCE | XNA's CopyData writes elementCount elements of sizeof(T) bytes spaced vertexStride apart and requires only vertexStride >= sizeof(T). CNA's raw transfer requires the stride to equal the buffer's declaration stride, the byte count to be exactly count*stride, and a windowed offset to be stride-aligned (build-probe/f60_stride.c, ten triples). Every packed transfer is expressible exactly; one that leaves a gap between elements is refused on the runtime channel AFTER every XNA validation, so a call XNA would reject still reports XNA's exception. |
| Static buffer upload route | VERIFIED_NATIVE | Cna_vertex_buffer_set_data_raw_at_with_options answers NOT_SUPPORTED on a static vertex buffer for every option value including CNA_SET_DATA_NONE, and succeeds on a dynamic one; a dynamic index buffer accepts Discard through cna_index_buffer_set_data and refuses it through _set_data_at (build-probe/f60_options.c). So the static path uses the option-free routes and SetDataOptions reaches the family only through the dynamic buffers. |
| System.Type-taking buffer constructors | LANGUAGE_MAPPING_LIMITATION | VertexBuffer(GraphicsDevice, Type, ...) needs Activator.CreateInstance and IndexBuffer(GraphicsDevice, Type, ...) needs Marshal.SizeOf, on a metatype. Swift has neither, and IVertexType.VertexDeclaration is an instance member, so the four vertex structs and the four CLR index widths are recognised by identity -- returning the same static declaration object XNA's FromType cache would hold -- and any other type reaches the VertexDeclaration- and IndexElementSize-taking constructors beside them. A type that IS an IVertexType but is not one of the four gets the limitation on the runtime channel, not XNA's VertexTypeNotIVertexType, because that message would be false. |
| Dynamic buffer streaming options | VERIFIED_NATIVE | DynamicVertexBuffer and DynamicIndexBuffer accept all three SetDataOptions and write what they were given, asserted by reading the contents back. The option's only managed consequence in XNA's CopyData is skipping the bound-buffer test that raises ResourceInUse; the rest is a D3D lock hint. The hint is forwarded where CNA takes it -- always on a dynamic vertex buffer, and on a whole-buffer write for a dynamic index buffer -- and dropped on a windowed index write, which CNA refuses (build-probe/f60_options.c). |
| Dynamic buffer ContentLost | VERIFIED_NATIVE_SUBSCRIPTION | Cna_vertex_buffer_subscribe_content_lost and its index counterpart create a real registration that the tests assert non-zero while the buffer lives and zero after disposal, on both the disposing and the finalizer path. The event is never RAISED on this host: CNA's header says it raises ContentLost only on renderer families whose API can lose a device, and documents is_content_lost as currently always false. XNA additionally latches the flag from GraphicsDevice.IsDeviceLost, which has no counterpart here -- the same divergence RenderTarget2D.IsContentLost records. |
| SpriteBatch Begin/Draw/End | VERIFIED_NATIVE | Begin, both submit shapes -- scaled and destination-rectangle -- and end execute for 60/600 template frames and 20 ownership cycles; all seven XNA Draw overloads execute inside a pair. |
| Pixel readback | UNSUPPORTED_BY_RENDERER | Cna_graphics_device_get_backbuffer_data_window and cna_texture2d_get_data_rgba8 both answer CNA_RESULT_NOT_SUPPORTED on the qualified HEADLESS artifact, so no test here can assert that a pixel ended up anywhere: build-probe/f53_readback.c and build-probe/f53_rtread.c. |
| Keyboard | VERIFIED_NATIVE | Native HEADLESS state route executes; empty state is observed backend evidence, not fabricated. |
| ButtonState | VERIFIED_MANAGED | Exact non-flags Int32 values and managed value behavior. |
| Buttons | VERIFIED_MANAGED | Exact Int32 OptionSet constants, arbitrary combinations, and verifier flags coverage. |
| GamePadButtons | VERIFIED_MANAGED | 11 physical properties plus exact equality, hash, string, and copy behavior. |
| GamePadDPad | VERIFIED_MANAGED | Asymmetric constructor/property ordering plus exact equality, hash, string, and copy behavior. |
| GamePadDeadZone | VERIFIED_MANAGED | Exact None, IndependentAxes, and Circular Int32 values. |
| GamePadState | VERIFIED_MANAGED | Both constructors, connection/packet defaults, every physical and virtual button, combinations, equality, hash, string, and copy behavior. |
| GamePadThumbSticks | VERIFIED_MANAGED | Exact square clamp, special binary32 behavior, equality, hash, string, and copies. |
| GamePadTriggers | VERIFIED_MANAGED | Exact clamp order, special binary32 behavior, equality, hash, string, and copies. |
| GamePadType | VERIFIED_MANAGED | Ten exact Int32 identities including BigButtonPad=768. |
| GamePadCapabilities value representation | VERIFIED_MANAGED | 26 read-only properties, value copies, and no public initializer. |
| GamePad.GetState default | VERIFIED_NATIVE_ROUTE | Canonical cna_gamepad_get_state returned a real disconnected IndependentAxes snapshot; positive controller state HARDWARE_PENDING. |
| GamePad.GetState None | VERIFIED_NATIVE_ROUTE | Canonical explicit dead-zone route returned a real disconnected snapshot; physical analog boundaries HARDWARE_PENDING. |
| GamePad.GetState IndependentAxes | VERIFIED_NATIVE_ROUTE | Canonical explicit dead-zone route returned a real disconnected snapshot; physical analog boundaries HARDWARE_PENDING. |
| GamePad.GetState Circular | VERIFIED_NATIVE_ROUTE | Canonical explicit dead-zone route returned a real disconnected snapshot; physical analog boundaries HARDWARE_PENDING. |
| GamePad.GetCapabilities | VERIFIED_NATIVE_ROUTE | Canonical per-control snapshot route returned real disconnected values; positive capability diversity HARDWARE_PENDING. |
| GamePad.SetVibration | VERIFIED_NATIVE_ROUTE | Canonical route returned applied=false for disconnected slot; positive physical rumble HARDWARE_PENDING. |
| Visible renderer output | BACKEND_BLOCKED | Qualified renderer is HEADLESS; native graphics submission is verified but no window/pixels are claimed. |
| Content/XNB | UNIMPLEMENTED_CNA_SWIFT | Deferred; fake ContentManager was removed. |
| Effects/3D | UNIMPLEMENTED_CNA_SWIFT | Deferred; fake BasicEffect/cube/capability route was removed. |
| Audio | UNIMPLEMENTED_CNA_SWIFT | Outside Foundation-1 selected profile. |
| Media | UNIMPLEMENTED_CNA_SWIFT | Outside Foundation-1 selected profile. |
| Storage | UNIMPLEMENTED_CNA_SWIFT | Outside Foundation-1 selected profile. |
| Canonical CNA HEAD C API build | VERIFIED_NATIVE | Resolved upstream: the Foundation-11 blocker was a missing GameUpdateRequiredException.hpp at CNA HEAD 1bb2145. The current C API builds at cnanext HEAD 0a6158e4f, and the admitted 0.21.0 artifact is byte-identical to cnanext/cmake-build-headless with headers byte-identical to that HEAD and exactly the 4,054 exports in cnanext tools/c-api/abi_baseline.json. |
| Linux runtime | VERIFIED_NATIVE | Swift 6.0.3 x86_64-pc-linux-gnu, exact ABI-0.7 HEADLESS/NULL. |
| macOS runtime | PLATFORM_PENDING | Not compiled or executed; no support claim. |
| iOS/tvOS/visionOS runtime | PLATFORM_PENDING | Not compiled or executed; package platform declarations removed. |
| Windows runtime | PLATFORM_PENDING | No LoadLibrary implementation or qualification. |
| Browser/Wasm runtime | PLATFORM_PENDING | No Swift/Wasm CNA artifact or loader qualification. |
| Host GameTime under a fixed time step | VERIFIED_NATIVE_WITH_MEASURED_XNA_DIVERGENCE | CNA 0.21.0 issues one leading frame whose Update carries elapsed=0 and then reproduces pinned XNA Game.Tick exactly: Update sees ElapsedGameTime == TargetElapsedTime and the pre-increment TotalGameTime, Draw sees the advanced TotalGameTime. Pinned XNA returns from Tick without calling Update or DrawFrame when the accumulated/target step count is zero, so no XNA callback observes a zero elapsed time; the leading frame is an upstream divergence this binding reports rather than corrects. Pinned by NativeLifecycleTests.testHostGameTimeSequenceIsMeasuredNotAssumed; derivation in docs/native-abi-migration-evidence.md. |
| RenderTarget2D construction, binding and consumption | VERIFIED_NATIVE | Cna_render_target2d_create succeeds on HEADLESS with renderer_available true; the granted kind, dimensions, level count, format, depth format, multisample count and usage are read back from cna_render_target_get_info; the same handle is accepted by cna_texture2d_get_info, cna_texture_get_info and cna_sprite_batch_submit_scaled_many, so the Texture2D substitutability is native rather than simulated; binding and restoring the backbuffer both succeed and destroying a bound target is refused with CNA_RESULT_INVALID_STATE. No pixel or visible output is claimed: the renderer is HEADLESS. |
| RenderTarget2D.ContentLost | VERIFIED_NATIVE_ROUTE_NO_POSITIVE_OBSERVATION | A real cna_render_target_subscribe_content_lost registration is created and released, and a test asserts both. The event is never RAISED on this host: HEADLESS is one of the renderer families CNA documents as unable to lose a device, so IsContentLost is always false. XNA additionally latches the flag from GraphicsDevice.IsDeviceLost on every read, which has no counterpart here; the divergence is recorded in docs/foundation-38-derivability-and-render-target-evidence.md. |
| Game timing, host state and the four host events | VERIFIED_NATIVE | Tick, SuppressDraw and ResetElapsedTime drive cna_game_tick, _suppress_draw and _reset_elapsed_time; IsActive, IsMouseVisible, IsFixedTimeStep, TargetElapsedTime and InactiveSleepTime are managed mirrors seeded from the host and moved only by a write the host accepted, because every one of those CLR getters is IL_NO_FAILURE_PATH and may not throw; Activated, Deactivated, Exiting and Disposed are four real cna_game_subscribe registrations, released at disposal. |
| Game.Exiting versus CNA's teardown notification | VERIFIED_NATIVE_WITH_MEASURED_RAISE_SITE | Measured apart with a pure-C probe: CNA_GameCallbacks.exiting fires at cna_game_request_exit AND again at cna_game_destroy for a game that never ran, while CNA_GAME_EVENT_EXITING fires only at cna_game_request_exit. XNA raises Exiting when the game is exiting and raises nothing when a game that never ran is disposed, so the event is the mapped one and the lifecycle callback maps to nothing. |
| IGraphicsDeviceService producer and DrawableGameComponent | VERIFIED_NATIVE | GraphicsDeviceManager registers itself in Game.Services under both IGraphicsDeviceManager and IGraphicsDeviceService, refusing a second manager with the assembly's own GraphicsDeviceManagerAlreadyPresent message; its device comes from cna_graphics_device_manager_get_graphics_device and its four IGraphicsDeviceService events are real cna_graphics_device_manager_subscribe registrations. Game.GraphicsDevice and DrawableGameComponent resolve that service out of the container exactly as XNA does. Registering into the managed container creates no second lifecycle: CNA's native game owns the device and reads nothing from GameServiceContainer. |
| Callback scope of a manager-supplied graphics device | VERIFIED_NATIVE | Measured with a pure-C probe: cna_graphics_device_manager_get_graphics_device answers CNA_RESULT_INVALID_STATE with a zero handle before a game runs and after it exits, and succeeds with a real handle inside a lifecycle callback. The binding's own isInsideCallback guard is therefore defence in depth over a host that already refuses. |
| Graphics state objects (Blend/DepthStencil/Rasterizer/Sampler) | VERIFIED_MANAGED_NOT_YET_BOUND_TO_A_DEVICE | All four types are complete managed Swift whose defaults and static presets are read out of the pinned XNA IL (SetDefaults and .cctor); ThrowIfBound reproduces the assembly's own BoundStateObject message. No device route is bound, so no state has yet been applied to a live CNA device and `isBound` is reachable only from within the module. |
| Use-after-dispose of a graphics resource | VERIFIED_NATIVE | Reading a disposed RenderTarget2D inside a live LoadContent callback raises CNAObjectDisposedException with ObjectName 'RenderTarget2D', the composed message "Cannot access a disposed object.\r\nObject name: 'RenderTarget2D'." and HResult 0x80131622 — the class XNA's own Helpers.CheckDisposed raises, on the projected-exception channel rather than on CNAError. System.ObjectDisposedException is admitted as the seventh BCL exception authority out of mscorlib 5634668d…acc63. |
| CNA graphics-device handle identity | MEASURED_NATIVE_DIVERGENCE | Measured with a pure-C probe: cna_game_get_graphics_device answers a handle that is stable within one lifecycle callback and DIFFERENT in every callback — LoadContent, the first Update, the first Draw and the third Update each answer a distinct value, and a second run continues the sequence. The handle is therefore a per-callback capability token and not the device's identity, so XNA's device-owned managed state (cachedBlendState and the sampler collections, which live as long as the device) cannot be anchored to the callback-scoped facade. No device-state member is projected yet; when one is, its storage must be anchored to the game, which is the object with the device's lifetime. |
| BlendFunction numbering across the boundary | MEASURED_NATIVE_DIVERGENCE | XNA's pinned IL declares BlendFunction.Min = 3 and Max = 4; CNA's graphics_state.h declares CNA_BLEND_FUNCTION_MAX = 3 and MIN = 4. The two are swapped, so a rawValue pass-through would silently exchange them. Every other state enum — Blend, ColorWriteChannels, CompareFunction, StencilOperation, CullMode, FillMode, TextureAddressMode and TextureFilter — agrees value for value. No state route is bound yet; when one is, the conversion must be an exhaustive explicit map for every enum rather than a raw cast. |
| VertexDeclaration and its validator | VERIFIED_MANAGED_NO_NATIVE_ROUTE | Complete managed Swift. The type-size table, the maximum-end-offset stride rule, the five validation failures and the order they are checked in all come from the pinned VertexElementValidator IL. CNA's own cna_vertex_declaration_create computes the same stride for four layouts chosen to separate a sum from a maximum and an ordered scan from an unordered one (build-probe/f43_stride.c) — corroboration, not authority. No cna_vertex_declaration_* route is bound, because no projected member consumes a declaration yet. |
| IVertexType and the four vertex structs | VERIFIED_MANAGED_WITH_A_TOOLCHAIN_LIMITATION | All five are complete managed Swift. The four static VertexDeclaration fields are built by the same one-argument constructor XNA's class constructors use, so their strides (16/24/32/20) are computed by Foundation 43's rule rather than transcribed. The limitation is the Swift 6.0.3 toolchain, not the projection: opening an `any Microsoft.Xna.Framework.Graphics.IVertexType` existential and reading its property crashes swift-frontend in IR generation with "Forming a CanType out of a non-canonical type!". Concrete access, the static field and a generic constraint all compile, and `any IGraphicsDeviceService` and `any IPackedVector` open fine, so it is neither existentials in general nor this binding's nested namespaces. A consumer on this toolchain must use a generic constraint. |
| Applying a graphics state to a live device | VERIFIED_NATIVE | The first state this binding has applied to anything. Assigning BlendState.Additive inside a LoadContent callback reaches CNA through cna_graphics_device_set_blend_state, the device caches the caller's own instance, the state comes back bound and refuses later writes, and set_BlendState's copied BlendFactor and MultiSampleMask move with it. SamplerStateCollection applies through cna_graphics_device_set_sampler_state on both stages. Eight routes bound, four PODs mirrored and compiler-verified field for field. Recorded divergences: both collections are 16 slots long because CNA accepts 0-15 on both stages while XNA's vertex collection is 0 under Reach and 4 under HiDef, and blendStateDirty/depthStencilStateDirty are transcribed but permanently false because neither a device reset nor an effect pass is projected. |
| Viewport, scissor rectangle and device status | VERIFIED_NATIVE | A viewport written through SetViewport reaches CNA and reads back through the reader that already existed (8,16 320x240 0.25..0.75 from an 800x480 default); a scissor rectangle round trips; the device reports Normal. GraphicsDeviceStatus is mapped explicitly even though CNA and XNA agree on all three values, because Foundation 45 found eight of nine state enums agreeing and the ninth swapped. On the qualified HEADLESS renderer the device is never lost, so Normal is the only value this environment can produce and no claim is made about Lost or NotReset. |

79 operations. The machine-readable source is
`docs/runtime-capabilities.json`.
