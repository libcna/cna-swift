{
  "schemaVersion": 1,
  "qualifiedEnvironment": {
    "platform": "Linux x86-64",
    "swift": "6.0.3",
    "target": "x86_64-pc-linux-gnu",
    "cnaAbi": "0.21.0",
    "renderer": "HEADLESS",
    "audio": "SDL3",
    "cnaAbiAdmittedWindow": "major 0, minor >= 21",
    "cnaSourceRevision": "0a6158e4ff764907065cd7259e3d29e331a52088",
    "cnaLibrarySha256": "c32bfbd307d695664f906ccf2834ec3f9ebc240fa388d544ac21ee3ebaeb731b"
  },
  "capabilities": [
    {
      "operation": "XNA namespace projection",
      "status": "VERIFIED_MANAGED",
      "evidence": "Swift Symbol Graph emits the strict Microsoft.Xna.Framework path; marker symbols are measured exclusions"
    },
    {
      "operation": "Pure math/value foundation",
      "status": "VERIFIED_MANAGED",
      "evidence": "MathHelper, Point, Rectangle, GameTime, the binary32/geometry closure, Color, and both packed-vector protocols are strict-complete managed Swift"
    },
    {
      "operation": "DisplayOrientation",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact root-framework Int32 OptionSet with Default=0, LandscapeLeft=1, LandscapeRight=2, and Portrait=4; no runtime orientation claim"
    },
    {
      "operation": "BufferUsage",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics Int32 OptionSet with None=0 and WriteOnly=1; no buffer or GPU resource capability is claimed"
    },
    {
      "operation": "DepthFormat",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics non-flags Int32 enum with None=0, Depth16=1, Depth24=2, and Depth24Stencil8=3; no depth buffer, stencil, render-target, GPU-format, or native capability is claimed"
    },
    {
      "operation": "FillMode",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics non-flags Int32 enum with Solid=0 and WireFrame=1; no RasterizerState, polygon mode, or wireframe rendering capability is claimed"
    },
    {
      "operation": "SurfaceFormat",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics non-flags Int32 enum with all twenty pinned literals; no texture, render-target, display, GPU-format, DXT, HDR, or native capability is claimed"
    },
    {
      "operation": "RenderTargetUsage managed enum contract",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics non-flags Int32 enum with DiscardContents=0, PreserveContents=1, and PlatformContents=2; no RenderTarget2D, RenderTargetCube, PresentationParameters, GraphicsDevice render-target operation, content discard/preserve/platform-defined semantics, or native capability is claimed"
    },
    {
      "operation": "GraphicsProfile managed enum contract",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics non-flags Int32 enum with Reach=0 and HiDef=1; no profile selection, GraphicsAdapter, GraphicsDeviceInformation, capability negotiation, or native capability is claimed"
    },
    {
      "operation": "Presentation and device-status managed enum contracts",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics enums PresentInterval (Default=0..Immediate=3), GraphicsDeviceStatus (Normal=0, Lost=1, NotReset=2) and PrimitiveType (TriangleList=0..LineStrip=3), plus the ClearOptions Int32 OptionSet (Target=1, DepthBuffer=2, Stencil=4); no present interval, vsync, swap chain, device-lost handling, device reset, Clear option path, DrawPrimitives, or native capability is claimed"
    },
    {
      "operation": "Render-state managed enum contracts",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics enums Blend, BlendFunction, CompareFunction, StencilOperation, CullMode, TextureFilter and TextureAddressMode, plus the ColorWriteChannels and SetDataOptions Int32 OptionSets; no BlendState, DepthStencilState, RasterizerState, SamplerState, blending, depth/stencil testing, culling, filtering, buffer update, or native capability is claimed"
    },
    {
      "operation": "Vertex declaration managed contracts",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics enums VertexElementFormat, VertexElementUsage and IndexElementSize, plus the VertexElement value struct with verbatim unvalidated construction, four-field equality, SmartGetHashCode word XOR and the exact reference ToString; no VertexDeclaration, IVertexType, VertexBuffer, IndexBuffer, vertex submission, or native vertex-format capability is claimed"
    },
    {
      "operation": "CubeMapFace managed enum contract",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics non-flags Int32 enum with PositiveX=0 through NegativeZ=5; no TextureCube, RenderTargetBinding, cube render target, cube sampling, or native face-mapping capability is claimed"
    },
    {
      "operation": "Effect parameter and effect interface managed contracts",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics enums EffectParameterClass and EffectParameterType, plus the IEffectFog and IEffectMatrices protocols with their exact abstract read/write property sets and no conformer; no Effect, EffectParameter, EffectAnnotation, BasicEffect, shader compilation, fog, applied matrix, or native capability is claimed"
    },
    {
      "operation": "Audio managed enum contracts",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Audio non-flags Int32 enums SoundState (Playing=0, Paused=1, Stopped=2) and AudioChannels (Mono=1, Stereo=2, no zero literal). AudioListener and AudioEmitter exist as pure managed 3D data that reproduce the pinned XACT handedness flip and hold no engine handle; no SoundEffect, SoundEffectInstance, Cue, playback, mixing, XACT interop or audio device capability is claimed, and the audio backend remains NULL"
    },
    {
      "operation": "CLR property accessor projection",
      "status": "VERIFIED_MANAGED",
      "evidence": "one accessor-level rule for every CLR property: the getter keeps Swift property or subscript syntax and gains `get throws` exactly when the XNA getter is fallible, and a setter Swift cannot express becomes Set<PropertyName> carrying the setter's own throws. Fallibility is derived from the CIL of the seven registered assemblies and pinned as tools/api_compat/reference/xna40-accessor-fallibility.json -- 114 fallible getters and 113 fallible setters of 840 properties -- and hash-checked by the verifier. This is a language mapping only: it claims no runtime, device or native behaviour, and the projected writer is the CLR setter accessor, not an added XNA member"
    },
    {
      "operation": "AudioEmitter managed 3D data",
      "status": "VERIFIED_MANAGED",
      "evidence": "the pinned XACT_EMITTER_DATA storage, the FlipHandedness involution and its negative-zero default Position.Z and Velocity.Z, and the exact ordered bge.un DopplerScale boundary in which NaN and -0.0 are accepted while every ordered negative throws. No XACT engine, voice, listener pairing or audio device is involved; Cue.Apply3D is not implemented, so nothing consumes an emitter and constructing one claims no audio capability"
    },
    {
      "operation": "DisplayMode managed descriptor contract",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Graphics class with six pinned identities, no public constructor, internal-only construction, verbatim Width/Height/Format storage, guarded binary32 AspectRatio, unmodified Windows TitleSafeArea rectangle, and exact ToString; no monitor enumeration, display-mode discovery, resolution switching, GraphicsAdapter, GraphicsDevice.DisplayMode, fullscreen mode management, or native display capability is claimed"
    },
    {
      "operation": "Game lifecycle",
      "status": "VERIFIED_NATIVE",
      "evidence": "Initialize/LoadContent/BeginRun/Update/Draw/EndRun/UnloadContent/Exiting execute through CNA callbacks; 60/600 runs pass"
    },
    {
      "operation": "Game callback error containment",
      "status": "VERIFIED_NATIVE",
      "evidence": "Initialize, LoadContent, Update, Draw and UnloadContent errors are caught in C trampolines and rethrown at controlled Swift boundaries"
    },
    {
      "operation": "Game recreation",
      "status": "VERIFIED_NATIVE",
      "evidence": "20 create/run/exit/destroy cycles pass; stale generations reject use"
    },
    {
      "operation": "GraphicsDevice",
      "status": "VERIFIED_NATIVE",
      "evidence": "callback-scoped borrowed device returned by cna_game_get_graphics_device"
    },
    {
      "operation": "Clear",
      "status": "VERIFIED_NATIVE",
      "evidence": "all three XNA overloads execute through cna_graphics_device_clear_options; the manager-created device reports DepthFormat.Depth24, so Clear(Color) forwards Target|DepthBuffer"
    },
    {
      "operation": "Texture2D stream load",
      "status": "VERIFIED_NATIVE",
      "evidence": "Foundation.InputStream bytes decode through CNA; logo reports native 128x128 and stress fixture reports 1x1"
    },
    {
      "operation": "Texture2D PNG/JPEG encoding",
      "status": "VERIFIED_NATIVE",
      "evidence": "SaveAsPng and SaveAsJpeg execute through cna_texture2d_create_cpu_only_rgba8, _get_encoded_byte_count and _copy_encoded, and the emitted bytes are read back by a PNG decoder written from the format specification in the test target, which knows nothing about CNA: the authored texels come out texel for texel, a non-square target size appears in the IHDR, and the JPEG's own frame header carries the requested dimensions. XNA's rewrite of every alpha-zero texel to Color.Transparent is performed by this projection because CNA's encoder does not: build-probe/f59_encode.c authored (200,100,50,0) and an independent decode read (200,100,50,0) back. cna_texture2d_save_file exists and is deliberately unbound; no XNA member takes a path."
    },
    {
      "operation": "Texture2D decode resize and zoom",
      "status": "MEASURED_NATIVE_DIVERGENCE",
      "evidence": "FromStream(device, stream, width, height, zoom) passes CNA_Texture2DDecodeInfo, whose zoom flag is documented as cover-and-crop against fit -- the two operations XNA selects with XnaImageOperation.Scale|Crop and Scale. XNA's own resize is inside UnsafeNativeMethods.DecodeStreamToTexture, unmanaged code absent from the registered assembly, so there is NO pinned authority for the arithmetic and none is claimed. Measured in build-probe/f59_decode.c over sixteen targets from a 4x2 source: zoom=false ignores the requested HEIGHT entirely -- it grants the requested width with the height derived from the source aspect, so 8x2 grants 8x4 -- where XNA's Scale fits inside both; and zoom=true refuses 8x2 and 2x8 with CNA_RESULT_INVALID_ARGUMENT while granting 4x4, 5x5, 3x6, 4x8 and 16x8 exactly. Both are reported, not corrected: correcting them would replace a measured native operation with an invented managed resampler."
    },
    {
      "operation": "Dispose(Boolean) on the finalizer path",
      "status": "VERIFIED_MANAGED",
      "evidence": "GraphicsResource.Dispose(false) disposes and raises nothing, which is what the IL does: the disposing branch calls ~GraphicsResource() -- flag then Disposing -- and the finalizer branch calls !GraphicsResource() and Object.Finalize(), neither of which touches the delegate. Until Foundation 59 the projection raised the event on both paths, and Swift's lack of protected puts Dispose(false) within any consumer's reach. Texture2D and SpriteBatch now declare the override XNA declares, and RenderTarget2D's three-deep chain still releases its native content-lost subscription before the base disposes."
    },
    {
      "operation": "Vertex and index buffer contents",
      "status": "VERIFIED_NATIVE",
      "evidence": "VertexBuffer and IndexBuffer create through cna_vertex_buffer_create and cna_index_buffer_create and round-trip their contents: authored vertices come back vertex for vertex through cna_vertex_buffer_get_data_raw, indices at both widths through cna_index_buffer_get_data, and a windowed write at a byte offset leaves the rest of the buffer alone. A WriteOnly buffer refuses GetData with XNA's own NotSupportedException before the native call, which CNA also refuses natively. Nothing DRAWN with these buffers is claimed."
    },
    {
      "operation": "Vertex transfer stride",
      "status": "MEASURED_NATIVE_DIVERGENCE",
      "evidence": "XNA's CopyData writes elementCount elements of sizeof(T) bytes spaced vertexStride apart and requires only vertexStride >= sizeof(T). CNA's raw transfer requires the stride to equal the buffer's declaration stride, the byte count to be exactly count*stride, and a windowed offset to be stride-aligned (build-probe/f60_stride.c, ten triples). Every packed transfer is expressible exactly; one that leaves a gap between elements is refused on the runtime channel AFTER every XNA validation, so a call XNA would reject still reports XNA's exception."
    },
    {
      "operation": "Static buffer upload route",
      "status": "VERIFIED_NATIVE",
      "evidence": "cna_vertex_buffer_set_data_raw_at_with_options answers NOT_SUPPORTED on a static vertex buffer for every option value including CNA_SET_DATA_NONE, and succeeds on a dynamic one; a dynamic index buffer accepts Discard through cna_index_buffer_set_data and refuses it through _set_data_at (build-probe/f60_options.c). So the static path uses the option-free routes and SetDataOptions reaches the family only through the dynamic buffers."
    },
    {
      "operation": "System.Type-taking buffer constructors",
      "status": "LANGUAGE_MAPPING_LIMITATION",
      "evidence": "VertexBuffer(GraphicsDevice, Type, ...) needs Activator.CreateInstance and IndexBuffer(GraphicsDevice, Type, ...) needs Marshal.SizeOf, on a metatype. Swift has neither, and IVertexType.VertexDeclaration is an instance member, so the four vertex structs and the four CLR index widths are recognised by identity -- returning the same static declaration object XNA's FromType cache would hold -- and any other type reaches the VertexDeclaration- and IndexElementSize-taking constructors beside them. A type that IS an IVertexType but is not one of the four gets the limitation on the runtime channel, not XNA's VertexTypeNotIVertexType, because that message would be false."
    },
    {
      "operation": "Dynamic buffer streaming options",
      "status": "VERIFIED_NATIVE",
      "evidence": "DynamicVertexBuffer and DynamicIndexBuffer accept all three SetDataOptions and write what they were given, asserted by reading the contents back. The option's only managed consequence in XNA's CopyData is skipping the bound-buffer test that raises ResourceInUse; the rest is a D3D lock hint. The hint is forwarded where CNA takes it -- always on a dynamic vertex buffer, and on a whole-buffer write for a dynamic index buffer -- and dropped on a windowed index write, which CNA refuses (build-probe/f60_options.c)."
    },
    {
      "operation": "Dynamic buffer ContentLost",
      "status": "VERIFIED_NATIVE_SUBSCRIPTION",
      "evidence": "cna_vertex_buffer_subscribe_content_lost and its index counterpart create a real registration that the tests assert non-zero while the buffer lives and zero after disposal, on both the disposing and the finalizer path. The event is never RAISED on this host: CNA's header says it raises ContentLost only on renderer families whose API can lose a device, and documents is_content_lost as currently always false. XNA additionally latches the flag from GraphicsDevice.IsDeviceLost, which has no counterpart here -- the same divergence RenderTarget2D.IsContentLost records."
    },
    {
      "operation": "GraphicsDevice.GraphicsProfile",
      "status": "VERIFIED_NATIVE",
      "evidence": "cna_graphics_device_get_graphics_profile answers GraphicsProfile.Reach on the qualified artifact. The route is called once, when the first facade of a runtime is built, and the answer is held on RuntimeState -- get_GraphicsProfile is IL_NO_FAILURE_PATH, so a fallible call must not sit behind it, which is the same reason GraphicsDevice.PresentationParameters is deliberately absent. XNA reads it once at device creation for the same reason."
    },
    {
      "operation": "ProfileCapabilities table",
      "status": "VERIFIED_MANAGED",
      "evidence": "Thirty-two values per profile extracted from the class constructor of the hash-registered Microsoft.Xna.Framework.Graphics.dll by tools/api_compat/profile_capabilities.py, pinned in reference/xna40-profile-capabilities.json, and compared back against the Swift table field for field by Foundation62ProfileCapabilityTests. Nine messages left recorded-message-absences.json: Texture2D's six and the two buffer constructors' three. Reach's IndexElementSize32 is false, so a 32-bit index buffer is refused on this device -- three tests written in Foundations 60 and 61 asserted behaviour XNA does not have and were corrected."
    },
    {
      "operation": "Vertex and index buffer binding",
      "status": "VERIFIED_NATIVE",
      "evidence": "SetVertexBuffer, SetVertexBuffers, GetVertexBuffers and Indices bind through cna_graphics_device_set_vertex_buffers and _set_index_buffer, and the managed objects are held on RuntimeState because CNA publishes no route from a native object back to a handle and its header prescribes caching what you bind -- which is what XNA's DeviceResourceManager does. The tests assert object IDENTITY, including a buffer created in LoadContent and bound in Update, which is why the InvalidDevice test compares runtimes and not per-callback facades."
    },
    {
      "operation": "Drawing",
      "status": "BLOCKED_UPSTREAM_UNTIL_EFFECT",
      "evidence": "Every CNA draw route answers CNA_ERROR_CATEGORY_INTERNAL with \"GraphicsDevice::DrawPrimitives: no effect has been applied\" while no effect is applied (build-probe/f63_draw.c), and XNA's VerifyCanDraw raises CannotDrawNoShader for the same reason. Both agree, so the draw members wait for Effect rather than shipping as members that always fail on the runtime channel where XNA raises InvalidOperationException. The binding half does not depend on a shader and landed in Foundation 63."
    },
    {
      "operation": "SpriteBatch Begin/Draw/End",
      "status": "VERIFIED_NATIVE",
      "evidence": "begin, both submit shapes -- scaled and destination-rectangle -- and end execute for 60/600 template frames and 20 ownership cycles; all seven XNA Draw overloads execute inside a pair"
    },
    {
      "operation": "Pixel readback",
      "status": "UNSUPPORTED_BY_RENDERER",
      "evidence": "cna_graphics_device_get_backbuffer_data_window and cna_texture2d_get_data_rgba8 both answer CNA_RESULT_NOT_SUPPORTED on the qualified HEADLESS artifact, so no test here can assert that a pixel ended up anywhere: build-probe/f53_readback.c and build-probe/f53_rtread.c"
    },
    {
      "operation": "Keyboard",
      "status": "VERIFIED_NATIVE",
      "evidence": "native HEADLESS state route executes; empty state is observed backend evidence, not fabricated"
    },
    {
      "operation": "ButtonState",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact non-flags Int32 values and managed value behavior"
    },
    {
      "operation": "Buttons",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact Int32 OptionSet constants, arbitrary combinations, and verifier flags coverage"
    },
    {
      "operation": "GamePadButtons",
      "status": "VERIFIED_MANAGED",
      "evidence": "11 physical properties plus exact equality, hash, string, and copy behavior"
    },
    {
      "operation": "GamePadDPad",
      "status": "VERIFIED_MANAGED",
      "evidence": "asymmetric constructor/property ordering plus exact equality, hash, string, and copy behavior"
    },
    {
      "operation": "GamePadDeadZone",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact None, IndependentAxes, and Circular Int32 values"
    },
    {
      "operation": "GamePadState",
      "status": "VERIFIED_MANAGED",
      "evidence": "both constructors, connection/packet defaults, every physical and virtual button, combinations, equality, hash, string, and copy behavior"
    },
    {
      "operation": "GamePadThumbSticks",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact square clamp, special binary32 behavior, equality, hash, string, and copies"
    },
    {
      "operation": "GamePadTriggers",
      "status": "VERIFIED_MANAGED",
      "evidence": "exact clamp order, special binary32 behavior, equality, hash, string, and copies"
    },
    {
      "operation": "GamePadType",
      "status": "VERIFIED_MANAGED",
      "evidence": "ten exact Int32 identities including BigButtonPad=768"
    },
    {
      "operation": "GamePadCapabilities value representation",
      "status": "VERIFIED_MANAGED",
      "evidence": "26 read-only properties, value copies, and no public initializer"
    },
    {
      "operation": "GamePad.GetState default",
      "status": "VERIFIED_NATIVE_ROUTE",
      "evidence": "canonical cna_gamepad_get_state returned a real disconnected IndependentAxes snapshot; positive controller state HARDWARE_PENDING"
    },
    {
      "operation": "GamePad.GetState None",
      "status": "VERIFIED_NATIVE_ROUTE",
      "evidence": "canonical explicit dead-zone route returned a real disconnected snapshot; physical analog boundaries HARDWARE_PENDING"
    },
    {
      "operation": "GamePad.GetState IndependentAxes",
      "status": "VERIFIED_NATIVE_ROUTE",
      "evidence": "canonical explicit dead-zone route returned a real disconnected snapshot; physical analog boundaries HARDWARE_PENDING"
    },
    {
      "operation": "GamePad.GetState Circular",
      "status": "VERIFIED_NATIVE_ROUTE",
      "evidence": "canonical explicit dead-zone route returned a real disconnected snapshot; physical analog boundaries HARDWARE_PENDING"
    },
    {
      "operation": "GamePad.GetCapabilities",
      "status": "VERIFIED_NATIVE_ROUTE",
      "evidence": "canonical per-control snapshot route returned real disconnected values; positive capability diversity HARDWARE_PENDING"
    },
    {
      "operation": "GamePad.SetVibration",
      "status": "VERIFIED_NATIVE_ROUTE",
      "evidence": "canonical route returned applied=false for disconnected slot; positive physical rumble HARDWARE_PENDING"
    },
    {
      "operation": "Visible renderer output",
      "status": "BACKEND_BLOCKED",
      "evidence": "qualified renderer is HEADLESS; native graphics submission is verified but no window/pixels are claimed"
    },
    {
      "operation": "Content/XNB",
      "status": "UNIMPLEMENTED_CNA_SWIFT",
      "evidence": "deferred; fake ContentManager was removed"
    },
    {
      "operation": "Effects/3D",
      "status": "UNIMPLEMENTED_CNA_SWIFT",
      "evidence": "deferred; fake BasicEffect/cube/capability route was removed"
    },
    {
      "operation": "Audio",
      "status": "UNIMPLEMENTED_CNA_SWIFT",
      "evidence": "outside Foundation-1 selected profile"
    },
    {
      "operation": "Media",
      "status": "UNIMPLEMENTED_CNA_SWIFT",
      "evidence": "outside Foundation-1 selected profile"
    },
    {
      "operation": "Storage",
      "status": "UNIMPLEMENTED_CNA_SWIFT",
      "evidence": "outside Foundation-1 selected profile"
    },
    {
      "operation": "Canonical CNA HEAD C API build",
      "status": "VERIFIED_NATIVE",
      "evidence": "resolved upstream: the Foundation-11 blocker was a missing GameUpdateRequiredException.hpp at CNA HEAD 1bb2145. The current C API builds at cnanext HEAD 0a6158e4f, and the admitted 0.21.0 artifact is byte-identical to cnanext/cmake-build-headless with headers byte-identical to that HEAD and exactly the 4,054 exports in cnanext tools/c-api/abi_baseline.json"
    },
    {
      "operation": "Linux runtime",
      "status": "VERIFIED_NATIVE",
      "evidence": "Swift 6.0.3 x86_64-pc-linux-gnu, exact ABI-0.7 HEADLESS/NULL"
    },
    {
      "operation": "macOS runtime",
      "status": "PLATFORM_PENDING",
      "evidence": "not compiled or executed; no support claim"
    },
    {
      "operation": "iOS/tvOS/visionOS runtime",
      "status": "PLATFORM_PENDING",
      "evidence": "not compiled or executed; package platform declarations removed"
    },
    {
      "operation": "Windows runtime",
      "status": "PLATFORM_PENDING",
      "evidence": "no LoadLibrary implementation or qualification"
    },
    {
      "operation": "Browser/Wasm runtime",
      "status": "PLATFORM_PENDING",
      "evidence": "no Swift/Wasm CNA artifact or loader qualification"
    },
    {
      "operation": "Host GameTime under a fixed time step",
      "status": "VERIFIED_NATIVE_WITH_MEASURED_XNA_DIVERGENCE",
      "evidence": "CNA 0.21.0 issues one leading frame whose Update carries elapsed=0 and then reproduces pinned XNA Game.Tick exactly: Update sees ElapsedGameTime == TargetElapsedTime and the pre-increment TotalGameTime, Draw sees the advanced TotalGameTime. Pinned XNA returns from Tick without calling Update or DrawFrame when the accumulated/target step count is zero, so no XNA callback observes a zero elapsed time; the leading frame is an upstream divergence this binding reports rather than corrects. Pinned by NativeLifecycleTests.testHostGameTimeSequenceIsMeasuredNotAssumed; derivation in docs/native-abi-migration-evidence.md"
    },
    {
      "operation": "RenderTarget2D construction, binding and consumption",
      "status": "VERIFIED_NATIVE",
      "evidence": "cna_render_target2d_create succeeds on HEADLESS with renderer_available true; the granted kind, dimensions, level count, format, depth format, multisample count and usage are read back from cna_render_target_get_info; the same handle is accepted by cna_texture2d_get_info, cna_texture_get_info and cna_sprite_batch_submit_scaled_many, so the Texture2D substitutability is native rather than simulated; binding and restoring the backbuffer both succeed and destroying a bound target is refused with CNA_RESULT_INVALID_STATE. No pixel or visible output is claimed: the renderer is HEADLESS"
    },
    {
      "operation": "RenderTarget2D.ContentLost",
      "status": "VERIFIED_NATIVE_ROUTE_NO_POSITIVE_OBSERVATION",
      "evidence": "a real cna_render_target_subscribe_content_lost registration is created and released, and a test asserts both. The event is never RAISED on this host: HEADLESS is one of the renderer families CNA documents as unable to lose a device, so IsContentLost is always false. XNA additionally latches the flag from GraphicsDevice.IsDeviceLost on every read, which has no counterpart here; the divergence is recorded in docs/foundation-38-derivability-and-render-target-evidence.md"
    },
    {
      "operation": "Game timing, host state and the four host events",
      "status": "VERIFIED_NATIVE",
      "evidence": "Tick, SuppressDraw and ResetElapsedTime drive cna_game_tick, _suppress_draw and _reset_elapsed_time; IsActive, IsMouseVisible, IsFixedTimeStep, TargetElapsedTime and InactiveSleepTime are managed mirrors seeded from the host and moved only by a write the host accepted, because every one of those CLR getters is IL_NO_FAILURE_PATH and may not throw; Activated, Deactivated, Exiting and Disposed are four real cna_game_subscribe registrations, released at disposal"
    },
    {
      "operation": "Game.Exiting versus CNA's teardown notification",
      "status": "VERIFIED_NATIVE_WITH_MEASURED_RAISE_SITE",
      "evidence": "measured apart with a pure-C probe: CNA_GameCallbacks.exiting fires at cna_game_request_exit AND again at cna_game_destroy for a game that never ran, while CNA_GAME_EVENT_EXITING fires only at cna_game_request_exit. XNA raises Exiting when the game is exiting and raises nothing when a game that never ran is disposed, so the event is the mapped one and the lifecycle callback maps to nothing"
    },
    {
      "operation": "IGraphicsDeviceService producer and DrawableGameComponent",
      "status": "VERIFIED_NATIVE",
      "evidence": "GraphicsDeviceManager registers itself in Game.Services under both IGraphicsDeviceManager and IGraphicsDeviceService, refusing a second manager with the assembly's own GraphicsDeviceManagerAlreadyPresent message; its device comes from cna_graphics_device_manager_get_graphics_device and its four IGraphicsDeviceService events are real cna_graphics_device_manager_subscribe registrations. Game.GraphicsDevice and DrawableGameComponent resolve that service out of the container exactly as XNA does. Registering into the managed container creates no second lifecycle: CNA's native game owns the device and reads nothing from GameServiceContainer"
    },
    {
      "operation": "Callback scope of a manager-supplied graphics device",
      "status": "VERIFIED_NATIVE",
      "evidence": "measured with a pure-C probe: cna_graphics_device_manager_get_graphics_device answers CNA_RESULT_INVALID_STATE with a zero handle before a game runs and after it exits, and succeeds with a real handle inside a lifecycle callback. The binding's own isInsideCallback guard is therefore defence in depth over a host that already refuses"
    },
    {
      "operation": "Graphics state objects (Blend/DepthStencil/Rasterizer/Sampler)",
      "status": "VERIFIED_MANAGED_NOT_YET_BOUND_TO_A_DEVICE",
      "evidence": "All four types are complete managed Swift whose defaults and static presets are read out of the pinned XNA IL (SetDefaults and .cctor); ThrowIfBound reproduces the assembly's own BoundStateObject message. No device route is bound, so no state has yet been applied to a live CNA device and `isBound` is reachable only from within the module."
    },
    {
      "operation": "Use-after-dispose of a graphics resource",
      "status": "VERIFIED_NATIVE",
      "evidence": "Reading a disposed RenderTarget2D inside a live LoadContent callback raises CNAObjectDisposedException with ObjectName 'RenderTarget2D', the composed message \"Cannot access a disposed object.\\r\\nObject name: 'RenderTarget2D'.\" and HResult 0x80131622 — the class XNA's own Helpers.CheckDisposed raises, on the projected-exception channel rather than on CNAError. System.ObjectDisposedException is admitted as the seventh BCL exception authority out of mscorlib 5634668d…acc63."
    },
    {
      "operation": "CNA graphics-device handle identity",
      "status": "MEASURED_NATIVE_DIVERGENCE",
      "evidence": "Measured with a pure-C probe: cna_game_get_graphics_device answers a handle that is stable within one lifecycle callback and DIFFERENT in every callback — LoadContent, the first Update, the first Draw and the third Update each answer a distinct value, and a second run continues the sequence. The handle is therefore a per-callback capability token and not the device's identity, so XNA's device-owned managed state (cachedBlendState and the sampler collections, which live as long as the device) cannot be anchored to the callback-scoped facade. No device-state member is projected yet; when one is, its storage must be anchored to the game, which is the object with the device's lifetime."
    },
    {
      "operation": "BlendFunction numbering across the boundary",
      "status": "MEASURED_NATIVE_DIVERGENCE",
      "evidence": "XNA's pinned IL declares BlendFunction.Min = 3 and Max = 4; CNA's graphics_state.h declares CNA_BLEND_FUNCTION_MAX = 3 and MIN = 4. The two are swapped, so a rawValue pass-through would silently exchange them. Every other state enum — Blend, ColorWriteChannels, CompareFunction, StencilOperation, CullMode, FillMode, TextureAddressMode and TextureFilter — agrees value for value. No state route is bound yet; when one is, the conversion must be an exhaustive explicit map for every enum rather than a raw cast."
    },
    {
      "operation": "VertexDeclaration and its validator",
      "status": "VERIFIED_MANAGED_NO_NATIVE_ROUTE",
      "evidence": "Complete managed Swift. The type-size table, the maximum-end-offset stride rule, the five validation failures and the order they are checked in all come from the pinned VertexElementValidator IL. CNA's own cna_vertex_declaration_create computes the same stride for four layouts chosen to separate a sum from a maximum and an ordered scan from an unordered one (build-probe/f43_stride.c) — corroboration, not authority. No cna_vertex_declaration_* route is bound, because no projected member consumes a declaration yet."
    },
    {
      "operation": "IVertexType and the four vertex structs",
      "status": "VERIFIED_MANAGED_WITH_A_TOOLCHAIN_LIMITATION",
      "evidence": "All five are complete managed Swift. The four static VertexDeclaration fields are built by the same one-argument constructor XNA's class constructors use, so their strides (16/24/32/20) are computed by Foundation 43's rule rather than transcribed. The limitation is the Swift 6.0.3 toolchain, not the projection: opening an `any Microsoft.Xna.Framework.Graphics.IVertexType` existential and reading its property crashes swift-frontend in IR generation with \"Forming a CanType out of a non-canonical type!\". Concrete access, the static field and a generic constraint all compile, and `any IGraphicsDeviceService` and `any IPackedVector` open fine, so it is neither existentials in general nor this binding's nested namespaces. A consumer on this toolchain must use a generic constraint."
    },
    {
      "operation": "Applying a graphics state to a live device",
      "status": "VERIFIED_NATIVE",
      "evidence": "The first state this binding has applied to anything. Assigning BlendState.Additive inside a LoadContent callback reaches CNA through cna_graphics_device_set_blend_state, the device caches the caller's own instance, the state comes back bound and refuses later writes, and set_BlendState's copied BlendFactor and MultiSampleMask move with it. SamplerStateCollection applies through cna_graphics_device_set_sampler_state on both stages. Eight routes bound, four PODs mirrored and compiler-verified field for field. Recorded divergences: both collections are 16 slots long because CNA accepts 0-15 on both stages while XNA's vertex collection is 0 under Reach and 4 under HiDef, and blendStateDirty/depthStencilStateDirty are transcribed but permanently false because neither a device reset nor an effect pass is projected."
    },
    {
      "operation": "Viewport, scissor rectangle and device status",
      "status": "VERIFIED_NATIVE",
      "evidence": "A viewport written through SetViewport reaches CNA and reads back through the reader that already existed (8,16 320x240 0.25..0.75 from an 800x480 default); a scissor rectangle round trips; the device reports Normal. GraphicsDeviceStatus is mapped explicitly even though CNA and XNA agree on all three values, because Foundation 45 found eight of nine state enums agreeing and the ninth swapped. On the qualified HEADLESS renderer the device is never lost, so Normal is the only value this environment can produce and no claim is made about Lost or NotReset."
    },
    {
      "operation": "TextureCube creation and description",
      "status": "VERIFIED_NATIVE",
      "evidence": "cna_texturecube_create answers a game-child handle and cna_texturecube_get_info reports the granted size, level count and format, inside a live LoadContent callback. Size, LevelCount and Format are what CNA granted, not what was asked for. The five profile refusals in front of it -- non-positive size, the ValidCubeFormats list, MaxCubeSize 512, the unconditional NonPow2Cube rule and the DXT alignment -- are managed and never reach a route."
    },
    {
      "operation": "TextureCube face data transfer",
      "status": "UNSUPPORTED_BY_RENDERER",
      "evidence": "cna_texturecube_set_data and cna_texturecube_get_data answer CNA_RESULT_NOT_SUPPORTED for all six faces on the qualified HEADLESS artifact (build-probe/f64_cube.c), which CNA's own header names a legitimate outcome: 'unsupported storage returns NOT_SUPPORTED atomically'. The six transfer members are implemented and every XNA validation in front of them is asserted; the transfer itself is asserted to be exactly that refusal on the runtime channel. NATIVE EVIDENCE, asserted nowhere: the same code round-trips all six faces on CNA's SOFTWARE artifact, which is not the qualified renderer."
    },
    {
      "operation": "Texture3D on this profile",
      "status": "VERIFIED_MANAGED",
      "evidence": "No Texture3D can be constructed on a Reach device and that is XNA's rule: ValidateCreationParameters raises ProfileFeatureNotSupported when MaxVolumeExtent is zero, and the table extracted from the pinned assembly gives Reach a zero extent and an empty ValidVolumeFormats. The type is complete and implemented, unreachable on this profile rather than absent. CNA agrees independently -- cna_texture3d_create answers NOT_SUPPORTED -- but the refusal raised is the managed one, not the native result code."
    },
    {
      "operation": "RenderTargetCube creation and binding",
      "status": "VERIFIED_NATIVE",
      "evidence": "cna_render_target_cube_create answers a game-child handle and cna_graphics_device_set_render_target_cube binds each of the six faces, inside a live LoadContent callback (build-probe/f65_rtcube.c). MEASURED DIVERGENCE: cna_texturecube_get_info accepts a render-target-cube handle and answers CNA_RESULT_SUCCESS with size 0, level count 0 and format 0, so the inherited cube route cannot describe this type; RenderTargetCube reads cna_render_target_get_info, which answers the real extents, level count, format, depth format, sample count and usage. A projection that read the inherited path would report a zero-sized cube with no call having failed."
    },
    {
      "operation": "Multiple render targets",
      "status": "VERIFIED_NATIVE_WITH_MEASURED_XNA_DIVERGENCE",
      "evidence": "cna_graphics_device_set_render_targets binds an array, cna_graphics_device_get_render_target_count answers its length and cna_graphics_device_copy_render_targets hands back each binding's handle, array slice and face -- measured for one 2D target, one cube face and TWO DISTINCT 2D targets (build-probe/f65_rtcube.c). The artifact accepts two; XNA does not, because Reach's MaxRenderTargets is 1, so the projection refuses at two with ProfileMaxRenderTargets and the native capability is never reached. CNA's own refusals were measured too and all three are documented: a duplicate target is INTERNAL with 'the same render-target subresource cannot be bound to more than one slot', a nonzero array_slice on a 2D target is NOT_SUPPORTED, and a non-positive-X face on a 2D target is INVALID_ARGUMENT."
    },
    {
      "operation": "Render-target binding readback",
      "status": "VERIFIED_MANAGED",
      "evidence": "GetRenderTargets hands back the managed RenderTargetBinding values that were bound, from a cache on RuntimeState. CNA can say which native handle occupies each slot but publishes no route from a native object back to a handle, so object identity -- which XNA's Array.Copy of currentRenderTargetBindings gives -- can only come from a cache. cna_graphics_device_copy_render_targets corroborates the handles and faces and is deliberately not bound: no projected member consumes it."
    },
    {
      "operation": "Runtime lifetime with a bound resource",
      "status": "MEASURED_NATIVE_DIVERGENCE",
      "evidence": "A GraphicsResource holds its GraphicsDevice facade strongly and the facade holds the RuntimeState strongly, so any cache on the runtime that holds a resource closes RuntimeState -> resource -> facade -> RuntimeState. Foundation 63 added the first such cache and binding one vertex buffer kept the runtime, every native handle it tracks and every registered child alive past Game.Dispose for the life of the process. RuntimeState.invalidateAfterNativeShutdown now releases every cache that holds a resource, which is also what XNA's GraphicsDevice.Dispose does. A retain cycle is invisible to AddressSanitizer; RuntimeLifetimeTests is what asks, with two controls proving the measurement can observe a released runtime at all."
    },
    {
      "operation": "Texture sampler slots",
      "status": "VERIFIED_NATIVE",
      "evidence": "cna_graphics_device_set_texture and _get_texture bind and read all 16 slots of both shader stages inside a live callback, and CNA_TextureSlotInfo::bound distinguishes 'occupied by something no handle names' from 'empty' (build-probe/f66_slots.c). The refusals were measured too: slot 16 is INVALID_ARGUMENT 'The texture sampler slot is out of range', an unknown stage is INVALID_ARGUMENT 'The shader stage is not recognized', and a texture that is the active render target is INVALID_STATE 'A texture that is currently bound as a render target cannot be bound for sampling' -- which is CNA's own version of XNA's MustResolveRenderTarget, reached only if the managed check were removed. Destroying a bound texture succeeds and empties every slot it held."
    },
    {
      "operation": "Texture collection slot counts",
      "status": "VERIFIED_MANAGED",
      "evidence": "RESOLVES the divergence recorded for the sampler collections. All four collections -- Textures, VertexTextures, SamplerStates and VertexSamplerStates -- are sized from the device's own profile: MaxSamplers 16 and MaxVertexSamplers 0 under Reach, 16 and 4 under HiDef, from the table extracted in Foundation 62. Until then all four were 16 long because the binding had no profile to ask, and a 16-slot vertex collection accepted fifteen indices XNA raises ArgumentOutOfRangeException for. CNA's own limit is the wider of the two -- CNA_TEXTURE_COLLECTION_MAX_TEXTURES is 16 on both stages -- and XNA's profile is the narrower rule that decides."
    }
  ]
}
